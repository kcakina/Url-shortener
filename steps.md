1. Clone repo

2. Create venv

3. Install deps (requirements.txt or minimal Flask)

4. Verify you can run something (hello world route)

5. Set up directory structure (only after you know it runs)

6. Data modeling

7. Business logic

8. Unit tests

9. PyCharm run configurations (only if needed; CLI first is faster)

10. Makefile (last; it’s packaging, no
::contentReference[oaicite:0]{index=0}
